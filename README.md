This application is recording contacts for user. It allows to send Sms and E-mail directly from the application to the contacts. Records saved to database and user can make CRUD operations
on records.
